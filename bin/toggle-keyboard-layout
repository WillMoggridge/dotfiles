#!/bin/bash

(setxkbmap -query | grep -q "layout:\s\+us") && setxkbmap gb || setxkbmap us -variant "colemak"

if [ -f ~/.Xmodmap ]; then
 xmodmap ~/.Xmodmap
fi


 #DEVICE="$(xinput -list | grep "TrulyErgonomic.com" | awk -F'=' '{print $2}' | cut -c 1-2 | head -1)"
 #setxkbmap -device $DEVICE us -variant colemak 
